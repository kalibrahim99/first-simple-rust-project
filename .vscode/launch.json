{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug executable",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/first_simple_rust_project",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "cargo build"
        }
    ]
}